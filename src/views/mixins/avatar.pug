mixin avatar(isName = true, style = "image")
  div&attributes(attributes).avatar-container
    if user.avatarUrl
      img(src=`${user.avatarUrl}` class=`avatar__${style}`)
    else
      div 😎
    if isName
     h3=user.name